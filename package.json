{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Difference calculator is a program that determines the difference between two data structures.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Oleg Mukhametshin",
  "license": "ISC",
  "bin": {
    "gendiff": "bin/gendiff.js"
  },
  "dependencies": {
    "commander": "^10.0.0"
  }
}
